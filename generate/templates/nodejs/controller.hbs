import schema from "./{{pascalCase name}}.model";
import { body } from "express-validator";
import { Request, Response } from "express";

export const create = [
	async (req: Request, res: Response) => {
		try {
			let {} = req.body;
		} catch (error) {
			console.log(error);
		}
	},
];

export const read = [
	async (req: Request, res: Response) => {
		try {
			let {} = req.body;
		} catch (error) {
			console.log(error);
		}
	},
];

export const update = [
	async (req: Request, res: Response) => {
		try {
			let {} = req.body;
		} catch (error) {
			console.log(error);
		}
	},
];

export const remove = [
	async (req: Request, res: Response) => {
		try {
			let {} = req.body;
		} catch (error) {
			console.log(error);
		}
	},
];