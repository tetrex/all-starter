import router from "express".Router();

import {read ,create , update , remove } from "./{{pascalCase name}}.controller";

router.get("/", read);
router.post("/", create);
router.put("/", update);
router.delete("/", remove);

export default router;