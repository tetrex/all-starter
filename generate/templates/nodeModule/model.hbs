import mongoose from "mongoose";

export interface I{{pascalCase name}} extends mongoose.Document {

  };
const {{pascalCase name}}Schema = new mongoose.Schema(
	{

	},
	{ timestamps: true }
);

const {{pascalCase name}}= mongoose.model<I{{pascalCase name}}>("{{pascalCase name}}", {{pascalCase name}}Schema);
export default {{pascalCase name}};

